
"""
@author: Masih
snakeFile for running the ml_benchmark_pipeline tools on BacGWASim-generated simulations
ای که خواهان تولدی دیگری, نخست مرگ را پذیرا باش
"""

from time import strftime, localtime


rule all:
     input:
          expand("{outDir}/mlSum.csv",outDir=config["outDir"])


#step1) convert replication to wildcard
include: 'modules/simConverter/sim2wildcard.snake', 
#step2) run GWAS tool 
include: 'modules/mlRun/gwasRun.snake',
#step3) evaluate the results
include: 'modules/mlEval/mlEval.snake',
#step4) summarize the results
include: 'modules/mlSum/mlSum.snake', 

